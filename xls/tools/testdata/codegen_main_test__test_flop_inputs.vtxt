module not_add(
  input wire clk,
  input wire [31:0] x,
  input wire [31:0] y,
  output wire [31:0] out
);
  // ===== Pipe stage 0:

  // Registers for pipe stage 0:
  reg [31:0] x__input_flop;
  reg [31:0] y__input_flop;
  always_ff @ (posedge clk) begin
    x__input_flop <= x;
    y__input_flop <= y;
  end

  // ===== Pipe stage 1:
  wire [31:0] p1_add_22_comb;
  wire [31:0] p1_not_27_comb;
  assign p1_add_22_comb = x__input_flop + y__input_flop;
  assign p1_not_27_comb = ~p1_add_22_comb;

  // Registers for pipe stage 1:
  reg [31:0] out__output_flop;
  always_ff @ (posedge clk) begin
    out__output_flop <= p1_not_27_comb;
  end
  assign out = out__output_flop;
endmodule
