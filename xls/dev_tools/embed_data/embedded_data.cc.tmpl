// {WARNING}
#include "{HEADER_FILE}"

#include "absl/types/span.h"

namespace {NAMESPACE} {

absl::Span<uint8_t const> {ACCESSOR}() {
  static constexpr uint8_t data[] = {
    {DATA_CONTENTS}
  };
  return absl::Span<const uint8_t>(data, sizeof(data));
}

}  // namespace {NAMESPACE}
